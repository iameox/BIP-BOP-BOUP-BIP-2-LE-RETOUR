repeat(fonction, min, max, chaine, index, resultat):
    i ← 0

    tant que i < max faire


    si i <= min alors resultat ← NIL
    sinon resultat ←


hex_val(chaine, index):
    c ← get_char(chaine, index)

    si c != 'x' retourner NIL
    c ← suivant(c)

    n ← repeat()
